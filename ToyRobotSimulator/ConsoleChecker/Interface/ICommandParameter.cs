namespace ToySimulator.ConsoleChecker.Interface
{
    /// <summary>
    /// This is an empty interface, used as a marker for the PlaceCommandParameter class (It is a pipe interface) It is called in the ICommandParameterChecker  and IInputParser interfaces.
    /// </summary>
    public interface ICommandParameter { }
}
